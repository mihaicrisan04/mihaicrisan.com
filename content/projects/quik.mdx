---
name: "quik"
slug: "quik"
shortDescription: "inline AI prompt for your zsh session — Cmd+Enter, ask, streams in place"
fullDescription: "a zsh plugin that turns your shell prompt into a quick AI ask. hit Cmd+Enter, type a question, get the answer streamed back inline — without leaving your terminal or breaking your shell flow. backed by Claude or Codex, written in Rust for instant startup."
status: "in-progress"
category: "tool"
featured: true
startDate: "2026-05-15"
techStack:
  - name: "Rust"
    category: "language"
  - name: "Zsh"
    category: "shell"
  - name: "Claude"
    category: "ai"
  - name: "Codex"
    category: "ai"
  - name: "Homebrew"
    category: "distribution"
links:
  - name: "github"
    url: "https://github.com/mihaicrisan04/quik"
    type: "github"
images: []
preview:
  video: "https://ik.imagekit.io/mihaicrisan/projects/quik/hero.mp4"
  image: "https://ik.imagekit.io/mihaicrisan/projects/quik/hero.mp4/ik-thumbnail.jpg"
highlights:
  - "Cmd+Enter from anywhere in your shell — no context switch"
  - "answers stream in place, no separate window or buffer"
  - "pluggable backend (Claude Code or OpenAI Codex on PATH)"
  - "native zsh plugin, distributed via homebrew tap"
  - "Rust core for sub-100ms startup"
---

inline AI in your terminal. hit `Cmd+Enter`, ask anything, get an answer streamed in place. built because i wanted Warp's inline-AI feature without giving up my actual terminal.

the friction it removes: every "quick question" — a flag i half-remember, a regex escape, a git error message i want explained — used to mean alt-tabbing to a browser, pasting, waiting, alt-tabbing back. quik kills the round trip and keeps the answer in the same buffer as the command it's about.

## install

```sh
brew install mihaicrisan04/tap/quik
```

then add to `~/.zshrc`:

```sh
source "$(brew --prefix)/share/quik/quik.plugin.zsh"
```

needs [`claude`](https://docs.claude.com/en/docs/claude-code) or [`codex`](https://github.com/openai/codex) on `PATH`.

## how the keybinding works

`quik` listens for the control byte `0x1f`. you map `Cmd+Enter` → that byte in your terminal of choice (Ghostty, iTerm, Kitty, WezTerm — see the wiki). the plugin handles everything else.

## design notes

the Rust core stays out of the hot path: the zsh plugin runs entirely in your shell session, only shelling out to the inference CLI when you actually ask. that means zero overhead on shell startup and no background daemons.

<ProjectLinks>
  <ProjectLinkButton type="github" href="https://github.com/mihaicrisan04/quik" />
</ProjectLinks>
